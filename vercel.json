{
  "framework": "nextjs",
  "buildCommand": "npm run prisma:generate && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm ci",
  "functions": {
    "src/app/**/*.ts": {
      "maxDuration": 30
    }
  }
}
